{
  "name": "memory_game",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7"
  },
  "scripts": {
    "dev": "BUN_PORT=8080 bun --hot ./src/index.html",
    "build": "bun build ./src/index.html --outdir dist --minify",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  }
}
